v#include<stdio.h>
int main()
{
    int x,y,z,a,b,count;
    count=0;
    scanf("%d %d",&a,&b);
    for(x=0;x<=a;x++)
       for(y=0;y<=a;y++)
    {
       z=b-(x+y);
    if(0<=z && z<=k)
        count++;
    }
    printf("%d",count);
    return 0;
}
